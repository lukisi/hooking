Part of Netsukuku.

This package is the module Hooking.
It depends on: tasklet-system, ntkd-common, ntkdrpc.

This package produces a convenience library for the daemon 'ntkd'.

The whole package is to be included into the package 'ntkd' by means
of the macro AC_CONFIG_SUBDIRS in configure.ac and the declaration
SUBDIRS in Makefile.am.

See documentation at https://github.com/lukisi/documentation
